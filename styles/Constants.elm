module Constants exposing (..)

import Css exposing (..)
import Css.Elements exposing (..)


boxShadow =
    boxShadow4 (px 3) (px 3) (px 5) (rgba 0 0 0 0.4)


textShadow =
    textShadow4 (px 3) (px 3) (px 5) (rgba 0 0 0 0.4)
